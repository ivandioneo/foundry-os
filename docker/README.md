# docker/

Local development infrastructure (PostgreSQL, MinIO, Mailpit) via Docker Compose.
No compose file is created in STORY-001 (structure only); it is added when the
database/storage/mail stories require it, to avoid provisioning services no code uses yet.
