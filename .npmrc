# Enforce the engines field in package.json (fail fast on wrong Node/pnpm).
engine-strict=true
# Pin exact versions on install — supply-chain hygiene / reproducible builds.
save-exact=true
# Resolve peer dependencies automatically (pnpm).
auto-install-peers=true
